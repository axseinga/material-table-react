const SearchBar = () => {
    return (
        <div>
            <label htmlFor="search">Search for:</label>
            <input name="search" type="text" />
        </div>
    );
};

export default SearchBar;
